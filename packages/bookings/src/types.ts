export type PluginTypes = {
  enabled: boolean;
  paymentsEnabled: boolean;
  childrenEnabled: boolean;
};

export type Lesson = {
  id: string;
  date: string;
  start_time: string;
  end_time: string;
};
