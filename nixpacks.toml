[phases.setup]
nixPkgs = ["nodejs_18", "pnpm-9_x", "openssl"]

[phases.install]
cmds = ["pnpm install"]

[phases.build]
cmds = ["pnpm run build --filter bru-grappling"]

[start]
cmd = "cd apps/bru-grappling && pnpm run start"

[variables]
NODE_ENV = "production"
