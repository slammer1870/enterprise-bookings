import type { Block } from 'payload'

export const DhLiveMembership: Block = {
  slug: 'dhLiveMembership',
  labels: {
    singular: 'Membership — subscribe / manage (tenant)',
    plural: 'Membership panels',
  },
  fields: [],
}
